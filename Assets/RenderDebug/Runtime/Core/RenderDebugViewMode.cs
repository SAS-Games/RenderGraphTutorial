namespace SAS.RenderDebugging
{
    /// <summary>Describes whether the registry is serving live or frozen frame data.</summary>
    public enum RenderDebugViewMode
    {
        Live,
        CapturePending,
        Captured
    }
}
